#include "holberton.h"
#include <stdio.h>

/**
 * print_rev - check the code for Holberton School students.
 * @s: char
 * Return: Always 0.
 */
void print_rev(char *s)
{
int d;
int g;
while (s[d] != '\0')
{
d++;
}
for (g = i - 1; g >= 0; g--)
{
_putchar(s[g]);
}
_putchar('\n');
}
